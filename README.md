# VYMS

Vehicle Yard Management System (Spring Boot + Thymeleaf).

## Quick Start

```bat
.\mvnw.cmd spring-boot:run
```

Open `http://localhost:8080` and sign in.

## Tests

```bat
.\mvnw.cmd test
```

## Profile

Signed-in users can update their profile at `/profile` (name, email, password).

